Summary
Adds unit tests for the modules that had the lowest coverage. Before, overall line coverage was 56.45% with several core files barely tested; these six files each had substantial untested logic. New specs bring them all to ~100% and raise overall line coverage to 69.48% (statements 56.28% → 68.23%).

Targeted files (line coverage before → after):

File	before	after
core/prisma/repositories/base.repository.ts	12.76%	100%
core/pagination/services/pagination.service.ts	15%	100%
modules/auth/repositories/auth.repository.ts	22.58%	100%
utils/files-uploads.utils.ts	0%	100%
utils/objects.utils.ts	0%	100%
core/email/services/email.service.ts	40%	100%
Tests only; no production code changed. Existing suites: 73 → 112 tests, all passing.

Coverage highlights per file:

base.repository: findOne/findAll (paginate on/off), findOneCached/findAllCached cache hit vs. miss (incl. cache-service-absent fallthrough and not-caching falsy results), invalidateModelCache (with/without id, no cache), softDelete (with/without prefix + invalidation), buildFilters.
pagination.service: buildUrl; createPaginator skip/take math and first/middle/last-page navigation URLs plus page/limit defaults.
auth.repository: getSessionInfo, updateMetadata, sendRecoveryMail (happy path + accepted:[] → InternalServerErrorException), resetPassword (happy path, token mismatch, verify failure — both wrapped as InternalServerErrorException). bcryptjs is mocked.
email.service, utils: both sendMail paths; nanoid filename with/without extension; isEmptyObject truthy/falsy/nullish.
Follows the repo's existing spec style (Test.createTestingModule with useValue mocks, reusing test-utils conventions).